# TechShare 启动说明

## 项目架构

```
浏览器 → Nginx(:8080) → app(:3000) → MySQL(:3306)
         (静态+反代)     (Express)    (数据)
```

三个容器各司其职：

| 容器 | 作用 | 端口 |
|------|------|------|
| `ts-mysql` | MySQL 8.4 数据库，存用户/帖子/资源元数据 | 3306（仅容器内网） |
| `app` | Node.js Express 后端，4 worker 模式 | 3000（仅容器内网） |
| `ts-nginx` | Nginx 反向代理 + 静态文件托管，对外入口 | 8080（浏览器访问） |

## 管理员账号

> 首次注册一个普通用户后，用 SQL 提权为 admin（密码不在文档/仓库中留存）：
> ```bash
> podman exec ts-mysql mysql -uroot -p"$DBPW" tech_share \
>   -e "UPDATE users SET role='admin' WHERE username='你的用户名';"
> ```
> 已有 admin 需轮换密码：登录后改密或按上条 SQL 更新 `password` 哈希（`bcryptjs.hash(新密码,10)` 生成）。

> 登录后请尽快修改密码。目前暂无在线改密接口，需要改密请手动执行：
> ```bash
> NEWPW="你的新密码"
> HASH=$(podman exec app node -e "require('bcryptjs').hash(process.env.PW,10).then(h=>console.log(h))" 2>/dev/null)
> DBPW="$(python3 -c "import os; print([l.split('=',1)[1] for l in open(os.path.expanduser('~/.config/techshare/.env'), encoding='utf-8-sig') if l.startswith('DB_PASSWORD=')][0])")"
> podman exec ts-mysql mysql -uroot -p"$DBPW" tech_share \
>   -e "UPDATE users SET password='$HASH' WHERE username='admin';"
> ```

## 首次启动（完整流程）

### 1. 配置环境变量

```bash
cd /path/to/project
cp backend/.env.example ~/.config/techshare/.env   # 配置住家目录，项目内不放 .env；记得 chmod 600
```

`~/.config/techshare/.env` 内容（照抄后改两个值）：

```
PORT=3000
DB_HOST=ts-mysql
DB_USER=root
DB_PASSWORD=改成你自己的强密码
DB_NAME=tech_share
DB_PORT=3306
CLUSTER=1
JWT_SECRET=改成随机长字符串（如 openssl rand -hex 32）
```

> **必改项**：`DB_PASSWORD`（MySQL 密码）和 `JWT_SECRET`（JWT 签名密钥），不改等于裸奔。

### 2. 构建应用镜像

```bash
podman build -t techshare-app -f Containerfile .
```

### 3. 创建容器网络（固定冷门网段，防校园网/VPN 撞车）

```bash
podman network create --subnet 192.168.77.0/24 --gateway 192.168.77.1 techshare
```

### 4. 启动 MySQL

```bash
podman run -d --name ts-mysql --network techshare \
  -e MYSQL_ROOT_PASSWORD="你的密码" \
  -e MYSQL_DATABASE=tech_share \
  -v ts-mysql-data:/var/lib/mysql \
  -v "$(pwd)/backend/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro,z" \
  docker.io/library/mysql:8.4
```

首次启动会自动建表（约 30s 就绪）。验证：
```bash
podman exec ts-mysql mysql -uroot -p"你的密码" -e "SHOW TABLES FROM tech_share;"
```

### 5. 启动 Redis（列表缓存用，无状态）

```bash
podman run -d --name ts-redis --network techshare docker.io/library/redis:alpine
```

### 6. 启动应用（8080 端口发布在这里，因为 Nginx 共享它的网络）

```bash
podman run -d --name app --network techshare \
  -p 8080:80 \
  --env-file ~/.config/techshare/.env \
  -e DB_HOST=ts-mysql \
  -e DB_PORT=3306 \
  -e CLUSTER=1 \
  -e CLUSTER_WORKERS=8 \
  localhost/techshare-app:latest
```

> 无需 `--ip`：Nginx 与 app 共享网络命名空间，upstream 走 `127.0.0.1`，网段随便换不断流。
> 配置里需有 `REDIS_URL=redis://ts-redis:6379`（没配则自动直查 DB，不报错）。

### 7. 启动 Nginx（必须在 app 之后，它共享 app 的网络）

```bash
podman run -d --name ts-nginx --network container:app \
  -v "$(pwd)/nginx.conf:/etc/nginx/nginx.conf:ro,z" \
  -v "$(pwd):/usr/share/web:ro,z" \
  localhost/techshare-nginx:latest
```

### 8. 打开浏览器

```
http://localhost:8080/
```

用管理员账号登录即可进入后台。

## 一键启动/停止

```bash
./start.sh    # 幂等：容器存在则复用，不存在则创建；自动等 MySQL 就绪
./stop.sh     # 停止（数据在卷里，重建即回）
./stop.sh --rm  # 停止并删除（下次 start.sh 从零创建，数据仍在卷里）
```

## 手动操作（不用脚本时）

容器停了之后按顺序恢复（先 app 后 nginx，因为 nginx 共享 app 的网络）：

```bash
podman start ts-mysql ts-redis app
sleep 5
podman start ts-nginx
```

彻底重建（比如改了代码）：`./stop.sh --rm && ./start.sh`。
> 删除顺序必须是先 `ts-nginx` 后 `app`（nginx 共享 app 的网络，反过来删会被依赖关系拦住）：

## 组网说明（校园网漫游安全）

- `techshare` 网段固定为 `192.168.77.0/24`（冷门，见 `compose.yaml`），避开校园 `10/8`、宿舍 `192.168.0/1`、VPN 段；
  换网段只改 `compose.yaml` 的 `networks` 一处 + `nginx.conf` 的 `resolver` 一行。
- Nginx 与 app 共享网络命名空间，upstream 走 `127.0.0.1` 长连接：网段/IP 怎么变都不用改配置。

## 压测成绩（2026-09-19，本机 20 核/15G，ab -k -c100）

| 接口 | QPS | 说明 |
|---|---|---|
| `/` 静态 | 29,900 | Nginx 直发 |
| `/healthz` | 36,658 | Nginx→Node，不查库 |
| `/api/resources` | 16,835 | Redis 缓存命中 |
| `/api/posts` | 25,452 | 同上 |
| `/api/comments` | 24,828 | 同上 |

读接口全部过万。实现手段：Redis 列表缓存（45s，按表版本失效）+ 8 worker + Nginx upstream 长连接。
已知约束：rootless podman 的 NAT 让所有外网 IP 看起来是一个，限流实质是全局的，
生产值 `LIMIT_API_PER_MIN=60000`（约 1000/s 熔断）；上传看带宽，不计入读 QPS。

## 健康检查

```bash
# 应用状态
curl http://localhost:8080/healthz
# → {"code":200,"msg":"ok"}

# 数据库连接
curl http://localhost:8080/api/health
# → {"code":200,"msg":"ok","db":"up"}

# 查看表结构
podman exec ts-mysql mysql -uroot -p"密码" -e "SHOW TABLES FROM tech_share;"
```

## 关键文件说明

| 文件 | 作用 |
|------|------|
| `server.js` | Express 后端主程序（API、鉴权、标准 S3 双轨上传） |
| `backend/schema.sql` | MySQL 建表脚本（5 张表 + 索引） |
| `backend/migrate-uploads-r2.js` | 一次性迁移脚本：本地 uploads/ → R2 |
| `Containerfile` | 应用镜像定义（node:24-slim，纯 JS 依赖） |
| `compose.yaml` | podman-compose 编排（装了 podman-compose 可直接用） |
| `nginx.conf` | Nginx 配置（静态缓存 + API 反代 + 限流） |
| `js/api.js` | 前端统一 API 封装（JWT 自动携带 + R2 直传 + 重试） |
| `~/.config/techshare/.env` | 环境变量（住家目录，不进项目不进 git，chmod 600） |

## 对象存储（R2 / 兼容 S3，可选）

都不配时上传走本地 `uploads/`，服务正常运行。配好后自动走直传。

### 方案 R2

1. Cloudflare 控制台建好 bucket，拿到 `Account ID`、`Access Key`、`Secret`、`bucket 名`、`自定义域名`
2. 填入 `~/.config/techshare/.env`：
   ```
   R2_ACCOUNT_ID=你的AccountID
   R2_ACCESS_KEY_ID=你的AccessKey
   R2_SECRET_ACCESS_KEY=你的Secret
   R2_BUCKET=你的bucket名
   R2_PUBLIC_URL=https://你的自定义域名
   ```
3. 重启应用：`podman restart app`

### 方案 兼容 S3（自建 MinIO 等标准 S3）

1. 填入 `~/.config/techshare/.env`（R2_* 留空）：
   ```
   S3_ENDPOINT=https://s3.example.com
   S3_REGION=auto
   S3_BUCKET=你的桶名
   S3_ACCESS_KEY_ID=你的AccessKey
   S3_SECRET_ACCESS_KEY=你的Secret
   S3_FORCE_PATH_STYLE=true
   S3_PUBLIC_URL=桶的公开访问域名（无则留空，下载走临时签名）
   ```
   诊断存储连通性（只打状态码，不泄露密钥）：
   ```bash
   podman exec app node /app/backend/s3check.js
   ```
2. 重启应用：`podman restart app`
3. 验证：share 页传一个小文件，能下载打开即成功

> 历史说明：本站曾支持“数据胶囊”经 rclone 中转读写（`S3_VIA_RCLONE`），因配额实测（HeadObject）不可用导致配额可谎报绕过，
> 且中转占用服务器带宽，已于本版下线（含 `backend/sigv4.js`、镜像内 rclone）。如仍需对接非标准 S3，请先确认其支持
> 标准 SigV4 + HeadObject/ListObjectsV2，否则不要接入。

### 迁移存量文件（可选，配好存储后）

```bash
podman exec app node migrate-uploads-r2.js
```
脚本自动读 `S3_*`（没有则读 `R2_*`），把本地 `uploads/` 搬到存储并回填数据库。

## 常见问题

### Nginx 502 Bad Gateway

通常是 `app` 容器 IP 变了。重启顺序：先起 MySQL → 再起 app → 最后起 Nginx。

### 登录显示“服务器连接失败”

检查三个容器是否都在运行：`podman ps`。确认 `app` 的 healthz 正常：`curl http://localhost:8080/healthz`。

### 端口被占用

8080 被占就改 Nginx 映射端口：
```bash
podman rm -f ts-nginx
podman run -d --name ts-nginx --network techshare -p 9090:80 ...
```
然后访问 `http://localhost:9090/`。
